package LogicaCalculadoraSimple;import java.io.*;public class PluginClassLoader extends ClassLoader {    File directorio;    public PluginClassLoader (File dir) {		directorio = dir;	}    public Class loadClass (String nombre) throws ClassNotFoundException {       return loadClass(nombre, true);     }    //Este fragmento de codigo fue extraido de https://javaranch.com/journal/200607/Plugins.html    public Class loadClass (String classname, boolean resolve) throws ClassNotFoundException {      try {	        Class c = findLoadedClass(classname);	        if (c == null) {	          try { c = findSystemClass(classname); }	          catch (Exception ex) {}	        }	        if (c == null) {	          String filename = classname.replace('.',File.separatorChar)+".class";	          File f = new File(directorio, filename);	          int length = (int) f.length();	          byte[] classbytes = new byte[length];	          DataInputStream in = new DataInputStream(new FileInputStream(f));	          in.readFully(classbytes);	          in.close();	          c = defineClass(null, classbytes, 0, length);	        }	        if (resolve) resolveClass(c);	        return c;      }      catch (Exception ex) {     	  throw new ClassNotFoundException(ex.toString());       }    }}